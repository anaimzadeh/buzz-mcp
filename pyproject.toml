[project]
name = "buzz-submission-mcp"
version = "0.1.0"
description = "FastMCP server that turns Agilix Buzz submissions into human-readable Q&A reports."
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=0.4.1",
]
